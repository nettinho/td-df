defmodule TdDfTest do
  use ExUnit.Case
  doctest TdDf

  test "greets the world" do
    assert TdDf.hello() == :world
  end


  test "imports TdDf.Templates" do
    assert TdDf.hello() == :world
  end
end
